<Page x:Class="KivMusic.Views.User.PaySuccess"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KivMusic.Views.User"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PaySuccess">

    <Grid Background="Green">
        <StackPanel Orientation="Vertical" 
                    Margin="5" 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <Label Content="Успех!" 
                   FontSize="16"
                   Margin="5"
                   Width="300" HorizontalContentAlignment="Center"/>
            <Label Content="Ваш чек:"/>
            <TextBlock x:Name="txtBill" Text="" MinHeight="200" TextWrapping="Wrap"/>
            <Button Name="btnDownload" Content="Скачать" 
                    Margin="5" Click="btnDownload_Click"/>
            <Button Name="btnMainBuyWindow" Content="В главное меню" 
                    Margin="5" Click="btnMainBuyWindow_Click"/>
        </StackPanel>
    </Grid>
</Page>
